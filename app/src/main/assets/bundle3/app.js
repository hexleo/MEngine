
function init(param) {
    MLog("bundle3 app.js init");
}

function receiveFromWebView(data) {
    MLog("bundle3 app.js receiveFromWebView=" + data);
}

function finish() {
    MLog("bundle3 catch finish event");
}

